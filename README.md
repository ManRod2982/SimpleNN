# Simple Neural Network

Simple neural network implementation example on C++.

## Usage

To build this project:
```
mkdir build
cmake -B build
cmake --build build
```
To run the tests:
```
cd build
ctest
```

## Dependencies

This code uses Catch2 for testing as well as Cmake:

```
sudo apt install cmake
sudo apt install catch2
```
